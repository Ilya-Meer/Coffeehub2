import { StyleSheet } from 'aphrodite/no-important';
import { fontFamilyMain, baseSpacingUnit } from '../../../../global/styles';

export default StyleSheet.create({
  textArea: {
    fontFamily: fontFamilyMain,
    fontSize: `${baseSpacingUnit * 1.6}px`,
  },
});
